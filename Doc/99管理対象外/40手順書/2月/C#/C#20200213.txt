C#Practice

■デリゲート（別ファイルにパスを通す）
・A cA = new A();クラス宣言
・protected あるいは publicでアクセス制限なしに。
・デリゲートの型
Func <パラメータの型, 戻り値の型> func 名前 = (パラメータのリスト) =>
{
 return 戻り値;
 };
・例えばTは配列の型

■デリゲートを用いたメソッド呼び出し
・delegate 戻り値の型 デリゲート名(パラメーター);

		delegate void Del();
		
        static void Disp()
        {
            Console.WriteLine("Hello world");
        }

        static void a(Del call)
        {
            call();
        }
        
        ##メイン処理
        a.(Disp);
        
■別ファイルのインスタンス生成
・static void Main(string[] args)の内部に生成する必要がある、
・staticな空間は別扱い、staticでなければOutput.Program.outputというようにおかしなインスタンスが出来上がる。
・